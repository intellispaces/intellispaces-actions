package tech.intellispaces.actions.wrapper;

import tech.intellispaces.actions.Action5;

/**
 * The wrapper action that wraps the action of the {@link Action5 action form 5}.
 *
 * @param <R> the action return type.
 * @param <D1> the first processed data type.
 * @param <D2> the second processed data type.
 * @param <D3> the third processed data type.
 * @param <D4> the fourth processed data type.
 * @param <D5> the fifth processed data type.
 */
public interface WrapperAction5<R, D1, D2, D3, D4, D5> extends WrapperAction, Action5<R, D1, D2, D3, D4, D5> {
}
